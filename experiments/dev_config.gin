# -----------------------------------------------------------------------------
# ----------------------------         MAIN          --------------------------
# -----------------------------------------------------------------------------
# main.steps_per_epoch = 100
# main.epochs = 100
# main.pi_lr = 3e-4
# main.vf_lr = 1e-3
# main.lam = 0.97


# -----------------------------------------------------------------------------
# ----------------------------        MODELS         --------------------------
# -----------------------------------------------------------------------------
# Critic.hidden_sizes = (512, 256, 128, 64)
# Actor.hidden_sizes = (512, 256, 128, 64)


# -----------------------------------------------------------------------------
# ----------------------------       TRAINING        --------------------------
# -----------------------------------------------------------------------------
# train_critic.train_iterations = 80


# -----------------------------------------------------------------------------
# ------------------------------    ENVIRONMENT    ----------------------------
# -----------------------------------------------------------------------------
create_environment.name = 'CartPole-v0'
# create_environment.name = 'HalfCheetah-v2'
create_environment.gym_make_kwargs = {}
create_environment.save_videos = False
create_environment.wrapper_kwargs = {
    "directory": "./videos/",
    "force": True,
    "write_upon_reset": True
}


# -----------------------------------------------------------------------------
# ----------------------------        LOGGING        --------------------------
# -----------------------------------------------------------------------------
setup_logger.name = "refactor-test-run"
setup_logger.notes = "This is the refactor test run"
setup_logger.project = "vanilla-policy-optimization"
setup_logger.tags = ['tag1', 'tag2']
setup_logger.save_code = True
setup_logger.monitor_gym = True


# -----------------------------------------------------------------------------
# ----------------------------         UTILS         --------------------------
# -----------------------------------------------------------------------------
set_seed.seed = 1337